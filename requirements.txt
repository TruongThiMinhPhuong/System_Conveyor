# ============================================
# AI Fruit Sorting System - Python Dependencies
# For Raspberry Pi 4 (8GB RAM)
# ============================================

# Core Dependencies
numpy>=1.21.0
pillow>=9.0.0
pyyaml>=6.0
loguru>=0.6.0

# Computer Vision
opencv-python>=4.5.0

# AI Models
ultralytics>=8.0.0

# TensorFlow Lite (for MobileNetV2)
# Use tflite-runtime for lighter installation on Pi
tflite-runtime>=2.5.0

# Hardware Control (Raspberry Pi specific)
# NOTE: These should be installed via apt on Raspberry Pi OS:
#   sudo apt install python3-rpi.gpio python3-gpiozero python3-picamera2
# The packages below are fallbacks if apt packages aren't available
RPi.GPIO>=0.7.0
gpiozero>=1.6.0

# IMPORTANT: picamera2 requires libcamera which must be installed via apt:
#   sudo apt install python3-libcamera python3-picamera2
# Do NOT install picamera2 via pip if using system Python or venv with --system-site-packages

# Web Interface
flask>=2.0.0
flask-cors>=3.0.0
flask-socketio>=5.0.0
python-socketio>=5.0.0
eventlet>=0.33.0

# ============================================
# Optional/Development Dependencies
# (Install separately if needed)
# ============================================

# Training tools (only needed for training on Pi)
# torch>=1.10.0
# torchvision>=0.11.0
# matplotlib>=3.5.0
# scikit-learn>=1.0.0

# Annotation tool
# labelImg>=1.8.0
