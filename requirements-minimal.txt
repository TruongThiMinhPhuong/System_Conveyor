# Minimal Essential Requirements for Raspberry Pi 4
# Run: pip install -r requirements-minimal.txt
#
# NOTE: Create venv with system-site-packages:
#   python3 -m venv --system-site-packages venv
#   source venv/bin/activate
#
# Also install via apt (for camera support):
#   sudo apt install python3-libcamera python3-picamera2 python3-rpi.gpio

# Core
numpy
pillow
pyyaml

# Computer Vision
opencv-python

# AI Models
ultralytics
tflite-runtime

# Hardware (fallback - prefer apt packages)
RPi.GPIO
gpiozero

# Web Interface
flask
flask-cors
flask-socketio
eventlet
